spring:
  datasource:
    url: jdbc:mysql://trip-tune-mysql:3306/trip_tune_letter?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: dev_team
    password: triptuneletter2222
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
    database-platform: org.hibernate.dialect.MySQL8Dialect