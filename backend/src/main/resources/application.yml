spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trip_tune_letter?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: trip-tune-letter-dev-secret
  expiration-ms: 86400000

openai:
  api-key: "YOUR_OPENAI_API_KEY"